@model Bangazon.Models.OrderViewModels.OrderDetailViewModel

@{
    ViewData["Title"] = "Payment";
}

<h1>Apply Payment</h1>

<h4>Order</h4>
<hr />
<div>
    
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Order.DateCreated)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Order.DateCreated)
        </dd>
        @*<dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Order.OrderProducts)
        </dt>
        <dd class="col-sm-10">
            @foreach (var product in Model.Order.OrderProducts)
            {
                <div>
                    @Html.DisplayFor(Model => product.Product.Title)
                </div>
            }
        </dd>*@
    </dl>
</div>
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>            
            <div class="form-group">
                <label asp-for="UserPaymentTypes" class="control-label"></label>
                <select asp-for="UserPaymentTypes" class="form-control" asp-items="@Model.UserPaymentTypes"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Submit" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
